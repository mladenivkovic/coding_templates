# recurse through all these subdirs before processing this dir
SUBDIRS = src
# README and run.sh will be distributed and installed in `docdir`. _DATA files are not automatically part of dist, so we add dist_ prefix.
dist_doc_DATA = README run.sh
